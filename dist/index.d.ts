/**
 * Process
 * 0. Get the dna hash
 * 1. Get the wasm hashes for each of the zomes
 * 2. Fetch the UI bundles for each of the zomes
 * 3. Set the available blocks as the
 * 4. If there is some block already defined in the zome, download it and render it
 *   4.1 Else, display layout editor and save layout to the zome
 */
